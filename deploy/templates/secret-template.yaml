---
apiVersion: v1
data:
  KEYKYCHASH: {{ .Values.REDISPORT | toString | b64enc | quote }}
  KEYSESSIONSTORAGE: {{ .Values.REDISPORT | toString | b64enc | quote }}
  TAGMANAGER: {{ .Values.REDISURL | toString | b64enc | quote }}
  APIURLMORTGAGE: {{ .Values.REDISPORT | toString | b64enc | quote }}
kind: Secret
metadata:
  name: "{{ .Values.application_name }}-{{ .Values.enviroment_name }}"
  namespace: "{{ .Values.namespace_name }}"
type: Opaque
